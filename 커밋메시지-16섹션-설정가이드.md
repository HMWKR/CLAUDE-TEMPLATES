# 다른 프로젝트에 16개 섹션 커밋 메시지 시스템 설정 가이드

## 개요

CalcLab에서 구축한 **16개 필수 섹션 커밋 메시지 검증 시스템**을 다른 프로젝트에 적용하는 방법입니다.
(2026-01-12 업데이트: 프롬프트 품질 검수 섹션 추가)

---

## 1. 필요한 패키지 설치

```bash
npm install -D husky @commitlint/cli @commitlint/config-conventional
```

---

## 2. Husky 초기화

```bash
npx husky init
```

이 명령어가 수행하는 작업:
- `.husky/` 폴더 생성
- `package.json`에 `"prepare": "husky"` 스크립트 추가
- 기본 `pre-commit` 훅 생성

---

## 3. 파일 생성 (3개)

### 3.1 `.husky/commit-msg` 생성

```bash
npx --no -- commitlint --edit "$1"
```

### 3.2 `commitlint.config.cjs` 생성

```javascript
/**
 * 16개 필수 섹션 커밋 메시지 검증 규칙
 * (프롬프트 품질 검수 섹션 포함)
 */
module.exports = {
  extends: ['@commitlint/config-conventional'],

  rules: {
    // 타입 제한
    'type-enum': [
      2,
      'always',
      [
        'feat', 'fix', 'docs', 'style', 'refactor',
        'perf', 'test', 'build', 'ci', 'chore', 'revert',
      ],
    ],

    // 헤더 최대 길이
    'header-max-length': [2, 'always', 100],

    // 제목 비어있으면 안됨
    'subject-empty': [2, 'never'],

    // 한국어 커밋 메시지 지원 (대소문자 규칙 비활성화)
    'subject-case': [0],

    // 본문 필수
    'body-empty': [2, 'never'],

    // 커스텀 규칙: 16개 섹션 검증
    'claude-md-sections': [2, 'always'],
  },

  // 커스텀 플러그인
  plugins: [
    {
      rules: {
        'claude-md-sections': (parsed) => {
          const raw = parsed.raw || '';

          // 필수 섹션 정의 (16개)
          const requiredSections = [
            { name: '산출물', pattern: /## 산출물/i },
            { name: '변경 영향도', pattern: /## 변경 영향도/i },
            { name: '테스트 전략', pattern: /## 테스트 전략/i },
            { name: '롤백 계획', pattern: /## 롤백 계획/i },
            { name: '관련 이슈', pattern: /## 관련 이슈/i },
            { name: 'Breaking Changes', pattern: /## Breaking Changes/i },
            { name: '성능 벤치마크', pattern: /## 성능 벤치마크/i },
            { name: '아키텍처 다이어그램', pattern: /## 아키텍처 다이어그램/i },
            { name: 'UI/UX 와이어프레임', pattern: /## UI\/UX 와이어프레임/i },
            { name: '사고 여정', pattern: /## 사고 여정/i },
            { name: '원본 프롬프트', pattern: /### 원본 프롬프트/i },
            { name: '프롬프트 분석', pattern: /### 프롬프트 분석/i },
            { name: '최적화된 프롬프트', pattern: /### 최적화된 프롬프트/i },
            { name: '프롬프트 품질 검수', pattern: /## 프롬프트 품질 검수/i },
            { name: 'Co-Authored-By', pattern: /Co-Authored-By:/i },
          ];

          // 누락된 섹션 찾기
          const missing = requiredSections
            .filter((s) => !s.pattern.test(raw))
            .map((s) => s.name);

          if (missing.length > 0) {
            return [
              false,
              `\n❌ 필수 섹션 누락: ${missing.join(', ')}\n\n` +
                `📋 필수 구조 (16개 섹션):\n` +
                `   ## 산출물\n` +
                `   ## 변경 영향도\n` +
                `   ## 테스트 전략\n` +
                `   ## 롤백 계획\n` +
                `   ## 관련 이슈\n` +
                `   ## Breaking Changes\n` +
                `   ## 성능 벤치마크\n` +
                `   ## 아키텍처 다이어그램\n` +
                `   ## UI/UX 와이어프레임\n` +
                `   ## 사고 여정\n` +
                `   ### 원본 프롬프트\n` +
                `   ### 프롬프트 분석\n` +
                `   ### 최적화된 프롬프트\n` +
                `   ## 프롬프트 품질 검수\n` +
                `   Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>`,
            ];
          }

          return [true];
        },
      },
    },
  ],
};
```

> **주의**: ES Module 프로젝트(`"type": "module"`)에서는 반드시 `.cjs` 확장자 사용

### 3.3 `.gitmessage` 생성 (커밋 템플릿)

```
[type]: [한 줄 요약]

## 산출물
- [생성/수정된 파일]

## 변경 영향도
- **영향 범위**: [어떤 기능/모듈에 영향]
- **사용자 영향**: [사용자가 체감하는 변화]
- **위험도**: [높음/중간/낮음]

## 테스트 전략
- [ ] [테스트 항목 1]
- [ ] [테스트 항목 2]

## 롤백 계획
- **롤백 방법**: [git revert / 수동 / 마이그레이션]
- **의존성**: [롤백 시 고려사항]

## 관련 이슈
- Closes #[이슈번호]
- Related to #[이슈번호]

## Breaking Changes
- [호환성 깨지는 변경사항]
- [마이그레이션 가이드]

## 성능 벤치마크
| 항목 | Before | After |
|------|--------|-------|
| [측정항목] | [값] | [값] |

## 아키텍처 다이어그램
```
[ASCII 구조도/흐름도]
```

## UI/UX 와이어프레임
```
[ASCII 와이어프레임]
┌─────────────────────────────────┐
│  Header                         │
├─────────┬───────────────────────┤
│ Sidebar │  Main Content         │
└─────────┴───────────────────────┘
```

## 사고 여정 (Thinking Journey)

### 1. 문제 인식
> **처음 이해**: [사용자 요청을 처음 어떻게 해석했는지]
> **조사 후 발견**: [실제로 코드/상황을 살펴본 후 파악된 것]

### 2. 탐색 경로
```
[파일] → [발견한 것] → [다음 행동]
```

### 3. 고려한 대안들
| 대안 | 장점 | 단점 | 결정 |
|------|------|------|:----:|
| [방법 A] | [장점] | [단점] | ❌ |
| **[채택된 방법]** | [장점] | [단점] | ✅ |

### 4. 결정 근거
- **채택 이유**: [왜 이 방법을 최종 선택했는지]
- **거부 이유**: [왜 다른 방법들을 선택하지 않았는지]

### 5. 구현 타임라인
```
[1] [첫 번째 단계] → [결과/발견]
[2] [장애물 발생] → [해결 방법]
[3] [최종 단계] → [완료 ✅]
```

### 6. 핵심 통찰
> [이 작업을 통해 얻은 교훈, 재사용 가능한 패턴, 주의사항]

## 프롬프트 기록

### 원본 프롬프트
```
[사용자 입력 그대로]
```

### 프롬프트 분석
> **해석**: [1-2문장]
> **작업 유형**: [수렴적/발산적/혼합형]
> **제약 수준**: [높음/중간/낮음]
> **핵심 키워드**: [중요 단어]

### 최적화된 프롬프트
```
## 역할

## 목표

## 기술 요구사항

## 금지 사항
```

**개선 포인트**:
- [개선 내용]

## 프롬프트 품질 검수

### 계층 1: 구조적 완성도 (18점)
| 카테고리 | 항목 | 점수 | 비고 |
|:--------:|------|:----:|------|
| 명확성 | 단일목적/행동동사/모호성제거 | /3 | |
| 구체성 | 정량화/범위한정/예시포함 | /3 | |
| 맥락 | 배경설명/제약명시/기대산출물 | /3 | |
| 구조화 | 논리순서/계층구조/시각분리 | /3 | |
| 제약 | 금지사항/범위제한/우선순위 | /3 | |
| 검증 | 성공기준/체크리스트/완료조건 | /3 | |
| **소계** | | **/18** | **필수 100%** |

### 계층 2: 엔지니어링 기법 (8점)
- [ ] Role Prompting (1점)
- [ ] Chain of Thought (1점)
- [ ] Few-shot Learning (1점)
- [ ] Negative Constraints (1점)
- [ ] Output Formatting (1점)
- [ ] Self-Verification (1점)
- [ ] Task Decomposition (1점)
- [ ] Context Priming (1점)
- **소계**: /8

### 계층 3: 맥락 최적화 (6점)
| 카테고리 | 항목 | 점수 |
|:--------:|------|:----:|
| 프로젝트 맥락 | 코드베이스/아키텍처 | /2 |
| 사용자 맥락 | 의도파악/수준맞춤 | /2 |
| 도메인 맥락 | 용어/모범사례 | /2 |
| **소계** | | **/6** |

### 계층 4: 효과 검증 (4점)
- [ ] 예상 개선 (1점)
- [ ] 측정 가능성 (1점)
- [ ] 부작용 분석 (1점)
- [ ] 대안 비교 (1점)
- **소계**: /4

### 계층 5: 할루시네이션 검증 (12점)
| 분류 | 항목 | 점수 | 검증 결과 |
|:----:|------|:----:|----------|
| 코드/파일 | 파일존재/심볼존재/위치정확/관계정확 | /4 | [검증됨] |
| 기술 정보 | 버전정확/API정확/사용법정확/제약정확 | /4 | [문서확인] |
| 논리/추론 | 인과관계/일관성 | /2 | |
| 검증 프로세스 | 출처명시/불확실성표시 | /2 | |
| **소계** | | **/12** | |

### 최종 평가
| 계층 | 점수 | 가중치 |
|:----:|:----:|:------:|
| 1. 구조적 완성도 | /18 | 필수 100% |
| 2. 엔지니어링 기법 | /8 | |
| 3. 맥락 최적화 | /6 | |
| 4. 효과 검증 | /4 | |
| 5. 할루시네이션 검증 | /12 | 게이트키퍼 |
| **총점** | **/48** | **등급: [ ]** |

### 검증 로그
```
[YYYY-MM-DD HH:MM] Glob: [파일경로] → [결과] [마커]
[YYYY-MM-DD HH:MM] Grep: [검색어] → [결과] [마커]
[YYYY-MM-DD HH:MM] Read: [파일:라인] → [결과] [마커]
```

---
Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
```

---

## 4. Git 설정

```bash
# 커밋 템플릿 등록
git config commit.template .gitmessage
```

---

## 5. (선택) CI 검증 - GitHub Actions

`.github/workflows/commit-lint.yml` 생성:

```yaml
name: Commit Message Lint

on:
  pull_request:
    branches: [main, master]

jobs:
  commitlint:
    name: Validate Commit Messages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate PR commits
        run: npx commitlint --from ${{ github.event.pull_request.base.sha }} --to ${{ github.event.pull_request.head.sha }} --verbose
```

---

## 6. (선택) pre-commit 훅 추가

`.husky/pre-commit` 수정:

```bash
# 타입체크 (TypeScript 프로젝트)
npm run typecheck

# 또는 린트
npm run lint
```

---

## 빠른 설정 체크리스트

| 단계 | 명령어/파일 | 완료 |
|:----:|-------------|:----:|
| 1 | `npm install -D husky @commitlint/cli @commitlint/config-conventional` | [ ] |
| 2 | `npx husky init` | [ ] |
| 3 | `.husky/commit-msg` 생성 | [ ] |
| 4 | `commitlint.config.cjs` 생성 | [ ] |
| 5 | `.gitmessage` 생성 | [ ] |
| 6 | `git config commit.template .gitmessage` | [ ] |
| 7 | (선택) `.github/workflows/commit-lint.yml` | [ ] |

---

## 검증 방법

```bash
# 실패 테스트 (섹션 누락)
git commit -m "feat: test"
# → ❌ 필수 섹션 누락 오류

# 성공 테스트 (전체 구조)
git commit  # 템플릿 표시 → 작성 → 저장
# → ✅ 커밋 성공
```

---

## 커스터마이징

### 섹션 축소/확대

`commitlint.config.cjs`의 `requiredSections` 배열에서 필요한 섹션만 유지:

```javascript
// 예: 5개 핵심 섹션만 사용
const requiredSections = [
  { name: '산출물', pattern: /## 산출물/i },
  { name: '원본 프롬프트', pattern: /### 원본 프롬프트/i },
  { name: '프롬프트 분석', pattern: /### 프롬프트 분석/i },
  { name: '최적화된 프롬프트', pattern: /### 최적화된 프롬프트/i },
  { name: 'Co-Authored-By', pattern: /Co-Authored-By:/i },
];
```

### 영문 프로젝트용 섹션명 변경

```javascript
const requiredSections = [
  { name: 'Output', pattern: /## Output/i },
  { name: 'Impact Analysis', pattern: /## Impact Analysis/i },
  { name: 'Test Strategy', pattern: /## Test Strategy/i },
  // ...
];
```

---

## 복사용 파일 구조

```
your-project/
├── .husky/
│   ├── commit-msg          # commitlint 실행
│   └── pre-commit          # (선택) 타입체크/린트
├── .github/
│   └── workflows/
│       └── commit-lint.yml # (선택) CI 검증
├── .gitmessage             # 커밋 템플릿
├── commitlint.config.cjs   # 검증 규칙
└── package.json            # devDependencies 추가
```

---

## 5계층 프롬프트 품질 검수 시스템 요약

### 등급 체계

| 등급 | 점수 범위 | 의미 |
|:----:|:---------:|------|
| **S** | 46-48 | 최고 품질, 대시보드 우선 수집 |
| **A+** | 42-45 | 우수 |
| **A** | 38-41 | 양호 |
| **B+** | 34-37 | 보통 |
| **B** | 30-33 | 개선 필요 |
| **F** | <30 또는 L1 | 재작성 필요 |

### 게이트키퍼 규칙

| 레벨 | 심각도 | 감점 |
|:----:|:------:|:----:|
| L1 | 치명 | 즉시 F |
| L2 | 심각 | -4점 |
| L3 | 경고 | -2점 |
| L4 | 주의 | -1점 |

### 검증 마커

- `[검증됨]`: Glob/Grep/Read로 확인
- `[문서확인]`: 공식 문서에서 확인
- `[추정]`: 패턴 기반 추측
- `[미확인]`: 검증 필요
- `[가정]`: 전제 조건 명시

---

*가이드 작성일: 2026-01-12*
